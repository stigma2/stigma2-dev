define(['./module'],
    function(app) {
        'use strict';

        app.controller('DashboardGlusterfsCtrl', [
            '$scope', '$state',
            function($scope, $state) {
                //
            }
        ]);
    }
);
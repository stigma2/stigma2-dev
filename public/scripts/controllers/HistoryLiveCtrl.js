define(['./module'],
    function(app) {
        'use strict';

        app.controller('HistoryLiveCtrl', [
            '$scope', '$state',
            function($scope, $state) {
                //
            }
        ]);
    }
);
define(['./module'],
    function(app) {
        'use strict';

        app.controller('DashboardOverviewCtrl', [
            '$scope', '$state',
            function($scope, $state) {
                //
            }
        ]);
    }
);
define(['./module'],
    function(app) {
        'use strict';

        app.controller('IndexCtrl', [
            '$scope', '$state',
            function($scope, $state) {
            }
        ]);
    }
);
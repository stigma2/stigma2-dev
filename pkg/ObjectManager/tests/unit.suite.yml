# Codeception Test Suite Configuration
#
# Suite for unit (internal) tests.

class_name: UnitTester
modules:
    enabled:
        - Asserts
        - \Stigma\ObjectManager\Helper\Unit 
        - Laravel5
        - Filesystem
    config:
        Laravel5:
            bootstrap: bootstrap/app.php
            root: ../../
            environment_file: .env.testing
